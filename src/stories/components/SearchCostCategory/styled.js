// @flow

import styled from 'styled-components';

export const CostCategoryWrapper = styled.div`
  position: relative;
  width: inherit;
`;

export const SearchCategory = styled.span`
  position: absolute;
  width: 100%;
  height: ${p => p.height}px;
`;
