"use strict";
/**
 * Copyright Zendesk, Inc.
 *
 * Use of this source code is governed under the Apache License, Version 2.0
 * found at http://www.apache.org/licenses/LICENSE-2.0.
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.execute = void 0;
const commander_1 = require("commander");
const utils_1 = require("../../utils");
const execa_1 = __importDefault(require("execa"));
/**
 * Execute the `github-branch` command.
 *
 * @param {string} [path] Path to a git directory.
 * @param {Ora} [spinner] Terminal spinner.
 *
 * @returns {Promise<string>} The branch name provided by a CI environment
 * variable or extracted from the given git repository.
 */
const execute = async (path, spinner) => {
    let retVal = process.env.CIRCLE_BRANCH ||
        process.env.TRAVIS_PULL_REQUEST_BRANCH ||
        process.env.TRAVIS_BRANCH;
    if (!retVal) {
        const args = ['branch', '--show-current'];
        if (path) {
            args.unshift('-C', path);
        }
        try {
            const branch = await (0, execa_1.default)('git', args);
            retVal = branch.stdout.toString();
        }
        catch (error) {
            (0, utils_1.handleErrorMessage)(error, 'github-branch', spinner);
            throw error;
        }
    }
    return retVal;
};
exports.execute = execute;
exports.default = (spinner) => {
    const command = new commander_1.Command('github-branch');
    return command
        .description('output GitHub branch name for the repo')
        .arguments('[path]')
        .action(async (path) => {
        try {
            spinner.start();
            const branch = await (0, exports.execute)(path, spinner);
            if (branch) {
                (0, utils_1.handleSuccessMessage)(branch, spinner);
            }
            else {
                throw new Error();
            }
        }
        catch {
            spinner.fail('GitHub branch not found');
            process.exitCode = 1;
        }
        finally {
            spinner.stop();
        }
    });
};
