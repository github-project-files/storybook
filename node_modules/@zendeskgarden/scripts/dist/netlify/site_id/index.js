"use strict";
/**
 * Copyright Zendesk, Inc.
 *
 * Use of this source code is governed under the Apache License, Version 2.0
 * found at http://www.apache.org/licenses/LICENSE-2.0.
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.execute = void 0;
const commander_1 = require("commander");
const utils_1 = require("../../utils");
const execa_1 = __importDefault(require("execa"));
/**
 * Execute the `netlify-site-id` command.
 *
 * @param {Ora} [spinner] Terminal spinner.
 *
 * @returns {Promise<string>} The value provided by the `NETLIFY_SITE_ID`
 * environment variable or the value of the `netlify.siteid` git configuration
 * option.
 */
const execute = async (spinner) => {
    let retVal = process.env.NETLIFY_SITE_ID;
    if (!retVal) {
        try {
            const siteId = await (0, execa_1.default)('git', ['config', '--get', 'netlify.siteid']);
            retVal = siteId.stdout.toString();
        }
        catch (error) {
            (0, utils_1.handleErrorMessage)(error, 'netlify-site-id', spinner);
            throw error;
        }
    }
    return retVal;
};
exports.execute = execute;
exports.default = (spinner) => {
    const command = new commander_1.Command('netlify-site-id');
    return command.description('output Netlify site API ID').action(async () => {
        try {
            spinner.start();
            const siteId = await (0, exports.execute)(spinner);
            if (siteId) {
                (0, utils_1.handleSuccessMessage)(siteId, spinner);
            }
            else {
                throw new Error();
            }
        }
        catch {
            spinner.fail('Netlify site ID not found');
            process.exitCode = 1;
        }
        finally {
            spinner.stop();
        }
    });
};
